html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Shop
    body
        nav 
            a(href=`/shop/${userId}/products/create`) Create Product
        main 
        block content